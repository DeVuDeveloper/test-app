<%= simple_form_for [oven, cookie], html: { class: 'form-horizontal' } do |f| %>
  <%= form_error_notification(cookie) %>
  <%= form_group(f, :fillings, 'Fillings', 'text') %>
  <%= form_group(f, :quantity, 'Quantity', 'numeric') %>
  <%= form_actions(f) %>
<% end %>


<h3 class="mt-4">My Ovens</h3>
  <div class="row">
    <div class="col-md-6">
      <ul class="list-group ovens">
        <%= turbo_frame_tag "ovens" do %>
        <%= render @ovens %>
      <% end %>
    </ul>
  </div>
